"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const sort_obj_by_key_1 = require("./sort-obj-by-key");
const convertObjToQueryStr = function (object) {
    return Object.keys(object)
        .filter((key) => object[key] !== undefined)
        .map((key) => {
        let value = object[key];
        // Sort nested object
        if (value && Array.isArray(value)) {
            value = JSON.stringify(value.map((val) => (0, sort_obj_by_key_1.sortObjDataByKey)(val)));
        }
        // Set empty string if null
        if ([null, undefined, 'undefined', 'null'].includes(value)) {
            value = '';
        }
        return `${key}=${value}`;
    })
        .join('&');
};
exports.default = convertObjToQueryStr;
